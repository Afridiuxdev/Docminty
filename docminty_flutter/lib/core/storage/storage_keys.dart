class StorageKeys {
  StorageKeys._();

  // Tokens — match web localStorage keys for consistency
  static const String accessToken = 'docminty_access_token';
  static const String refreshToken = 'docminty_refresh_token';

  // User cache
  static const String userJson = 'docminty_user';
}
